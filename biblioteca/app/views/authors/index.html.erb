<h1><%= I18n.t(:list_authors) %></h1>


<div class = "row">
    <%= form_tag authors_path, method: :get do %>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Pesquisar" %>
    <% end %>
</div>



<% if params[:search].present? %>
      <div class = "row">
         <div class= "alert alert-success">
             <%= @authors.size %>  Resultados encontrados.
         </div>
      </div>
<% end %>




<% if flash[:notice] %>
<div class="alert alert-success">
  <%= flash[:notice] %>
</div>
<% end %>

<%= form_tag authors_path, method: :get do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Pesquisar" %>
<% end %>

<% if params[:search].present? %>
  <div class="alert alert-success">
    <%= @authors.size %> resultados encontrados.
  </div>
<% end %>

<div class="row">


<table class="table table-striped">
  <tr>
    <th>#</th>
    <th>Nome</th>
    <th>Data de Nascimento</th>
    <th></th>
  </tr>
  <% @authors.each do |author| %>
  <tr>
    <td><%= author.id %></td>
    <td><%= author.name %></td>
<<<<<<< HEAD
    <td><%= author.birthdate %></td>
    <td><%= link_to "Detalhes", author_path(author.id), class: "btn btn-xs btn-success" %>
        <%= link_to "Editar", edit_author_path(author.id), class: "btn btn-xs btn-primary" %>
      <!-- <td><%= link_to "Deletar", author_path(author.id), method: :delete, data: {confirm: "Voce realmente deseja apagar este autor?"} %></td> -->
      <%= link_to( "Remover", author_path(author.id), {method: :delete, class: "btn btn-xs btn-danger",
      data: {confirm: "Deseja realmente remover este Autor?" } }) %>
    </td>

  </tr>
  <% end %>
</table>

<%= will_paginate @authors %>
=======
    <td><%= I18n.l author.birthdate unless author.birthdate.nil? %></td>
    <td><%= I18n.l author.updated_at %></td>
    <td><%= I18n.l author.created_at %></td>
    <td><%= link_to "Detalhes", author_path(author.id), class: 'btn btn-xs btn-primary' %></td>
    <td><%= link_to "Editar", edit_author_path(author.id), class: 'btn btn-xs btn-success' %></td>
    <td><%= link_to "Deletar", author_path(author.id), method: :delete,
        data: {confirm: "Voce realmente deseja apagar este autor?"}, class: 'btn btn-xs btn-danger' %></td>
  </tr>
  <% end %>
</table>
</div>
>>>>>>> 591b2ac9269ab7abd01edc3f85ebb94f87ae6fd7

<%= will_paginate @authors %>


<%= link_to "Novo autor", new_author_path, class: "btn btn-primary" %>
